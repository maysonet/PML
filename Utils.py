def new_project(proj_name, proj_id, start_date, end_date):
    return 'Project was created: ' + proj_name + ', ' + proj_id + ', '+ start_date + ', ' + end_date


def add_user(username, proj_id):
    return 'User was added: ' + username + ', ' + proj_id
